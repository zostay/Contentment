<%args>
$form_id
$name
</%args>

<%perl>
use DateTime::Format::Strptime;
my $strp = DateTime::Format::Strptime->new(pattern  => "%Y-%m-%d %H:%M:%S");
my $value = $strp->parse_datetime($m->request_args->{"$form_id.$name"});
$context->add_form_value($form_id, $name, $value);
</%perl>
