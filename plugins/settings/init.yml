name: Settings
description: Used to store configuration. Required by Contentment.
version: 0.06
required: 1
order: -90

use_lib:
 - lib

use:
 - Contentment::Setting

hooks:
  Contentment::install: 
    order: -95
    sub: Contentment::Setting::install
