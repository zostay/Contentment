name: Settings
description: Used to store configuration. Required by Contentment.
version: 0.03
required: 1
order: -90

use_lib:
 - lib

use:
 - Contentment::Settings

hooks:
  Contentment::install: 
    order: -95
    sub: Contentment::Settings::install
