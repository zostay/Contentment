name: VFS
description: Used to find files. Required by Contentment.
version: 0.09
required: 1
order: -50

use_lib:
 - lib

use:
 - Contentment::VFS

hooks:
  Contentment::install: 
    order: -50
    sub: Contentment::VFS::install
  Contentment::Response::resolve: Contentment::VFS::resolve
