<div class="form">
% $context->start_widget_action($action, $url, $default_map);
% my $top = $context->is_top_form;
% if ($top) {
<form action="/content/widget/process" method="POST">
% }
<% $m->content %>
% if ($top) {
</form>
% }
% $context->end_widget_action;
</div>

<%args>
$action
$default_map
</%args>
